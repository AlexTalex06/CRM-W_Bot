{
  "private": true,
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.6.0",
    "openai": "^4.20.0"
  },
  "scripts": {
    "postinstall": "cd frontend && npm install --legacy-peer-deps && npm run build"
  }
}
